{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>
    <div class="pure-g">
		<div class="pure-u-1-3">
			<table class="pure-table" style="width: 100vw">
    
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

   
        <div class="containerBouton" style=" flex-direction: column; justify-content:center; margin-top: 20px;">

    <a href="{{ path('app_ville_index') }}" class="pure-button pure-button-primary;margin-top: 20px">back  to list</a>

    <a href="{{ path('app_ville_edit', {'id': ville.id}) }}" class="pure-button pure-button-primary; margin-top: 20px ">edit</a>
  </div>      
    {{ include('ville/_delete_form.html.twig') }}


{% endblock %}
