
  {# CONTAINER PRINCIPAL #}
  <div class="pure-g containerPrincipal" style="padding: 10px;">

    {# CONTAINER 1 - SORTIE #}
    <div class="pure-u-1-3 box">

{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="container-Bouton">
 
    <button class="pure-button pure-button-primary">{{ button_label|default('Enregistrer') }}</button>
    
    </div>
{{ form_end(form) }}

    </div>
  </div>