  {# CONTAINER PRINCIPAL #}
  <div class="pure-g containerPrincipal" style="padding: 10px;">

    {# CONTAINER 1 - SORTIE #}
    <div class="pure-u-1-3 box">

{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="containerBouton" style="display:flex; flex-direction: column; justify-content:center; margin-top: 20px;">
    <button class="btn">{{ button_label|default('Save') }}</button>
    </div>

    {{ form_end(form) }}
    </div>
  </div>

