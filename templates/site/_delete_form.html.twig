<form method="post" action="{{ path('app_site_delete', {'id': site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site.id) }}">

    <div class="containerBouton" style="display:flex; flex-direction: column; justify-content:center; margin-top: 20px;"></div>
    <button class="pure-button pure-button-primary" style="display: block;
    margin : auto;">Delete</button>
</div>
</form>
