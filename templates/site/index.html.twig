{% extends 'base.html.twig' %}

{% block title %}Site index
{% endblock %}

{% block body %}
	<h1>Site index</h1>
	<div class="pure-g">
		<div class="pure-u-1-3">
			<table class="pure-table" style="width: 100vw">
				<thead>
					<tr>
						<th>Id</th>
						<th>Nom</th>
						<th>actions</th>
					</tr>
				</thead>
				<tbody>
					{% for site in sites %}
						<tr>
							<td>{{ site.id }}</td>
							<td>{{ site.nom }}</td>
							<td>

								<div class="containerBouton"></div>
								<a href="{{ path('app_site_show', {'id': site.id}) }}" class="pure-button pure-button-primary">show</a>
								<a href="{{ path('app_site_edit', {'id': site.id}) }}" class="pure-button pure-button-primary">edit</a>
							</td>
						</tr>
					</div>
				</tbody>
			</td>
		</tr>
	{% else %}
		<tr>
			<td colspan="3">no records found</td>
		</tr>
	{% endfor %}
</tbody></table><div class="containerBouton"></div><a href="{{ path('app_site_new') }}" class="pure-button pure-button-primary" style="margin-top: 10px;">Créer un site</a></div></div></div>{% endblock %}
